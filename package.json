{
  "name": "sherlock",
  "version": "1.0.0",
  "description": "Tracked Entity Instance Duplicate Finder",
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "build": "d2-app-scripts build",
    "start": "d2-app-scripts start",
    "test": "d2-app-scripts test",
    "deploy": "d2-app-scripts deploy",
    "lint": "yarn d2-style check",
    "lint:staged": "yarn lint --staged",
    "format": "yarn d2-style apply",
    "format:staged": "yarn format --staged"
  },
  "devDependencies": {
    "@dhis2/cli-app-scripts": "^11.2.1",
    "@dhis2/cli-style": "^10.7.3"
  },
  "dependencies": {
    "@dhis2/app-runtime": "^3.10.4",
    "fuse.js": "^7.0.0",
    "lodash": "^4.17.21",
    "react-router-dom": "^6.23.1",
    "react-slider": "^2.0.6",
    "sherlock": "file:"
  }
}
